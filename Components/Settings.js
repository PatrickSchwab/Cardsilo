import React from 'react';
import {Image, View} from "react-native";

export const Settings = () =>{

    return(
        <View>
            <Image
                source={{
                    uri: 'https://logo.clearbit.com/spotify.com',
                }}
            />
        </View>
    );
}